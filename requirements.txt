# Vercel serverless deployment uses only the Python standard library.
# The full g4f dependency set (curl_cffi, fastapi, uvicorn, pystray, ...) is
# too heavy for Vercel serverless functions and is preserved in
# requirements-full.txt for local / Docker use:  pip install -r requirements-full.txt
